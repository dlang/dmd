Implements new C++ mangling syntax which allows for keywords to be used for mangling
and only changes the mangle of a symbol with no other side-effects.

---
extern(C++, "__traits") void foo();

extern(C++, "std", "chrono") void bar(); // multiple namespace syntax
---
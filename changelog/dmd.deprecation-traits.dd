An error is now issued for the use of `__traits(getAttributes)` on an overload set

This used to give a deprecation, it is now an error:
```
@("gigi")
void fun() {}
@("mimi")
void fun(int) {}

void main()
{
    // Error: `__traits(getAttributes)` may only be used for individual functions, not the overload set `fun`
    //        the result of `__traits(getOverloads)` may be used to select the desired function to extract attributes from
    auto t =  __traits(getAttributes, fun);
}
```

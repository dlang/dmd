`version` can now be used as an expression.

Instead of:

---
version(linux)
{
    enum value = 42;
}
else
{
    enum value = 0;
}
---

You can now do this instead:

---
enum value = version(linux) ? 42 : 0;
---

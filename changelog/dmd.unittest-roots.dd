Add new flag `-unittest-roots`

Besides the existing `-unittest` flag dmd now also takes the
accompanying flag `-unittest-roots`. When passed, only the unittests
placed in the source files `X`... being explicitly passed to the call
to dmd are processed like when `-unittest` is passed. All other
unittests placed in modules that are imported via `X`... either
implicitly or explicitly will be ignored.